# cp .env.example .env 后按需修改

# 镜像标签。cuda = 跟随最新版；想钉死用 v0.0.11-cuda
IMAGE_TAG=cuda

# 宿主机监听端口
HOST_PORT=8000

# GLM 等智谱 API 模型需要
ZHIPU_API_KEY=

# 服务自身的访问令牌。要生效还得在 server.yaml 里把
# security.api_key_enabled 设成 true
XANYLABELING_API_KEY=
